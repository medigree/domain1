name: CI
on:
  push:
    branches-ignore:
      - 'gh-pages'
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: hl7fhir/ig-publisher-base:latest
    steps:
      - name: Update the image to the latest publisher
        run: curl -L https://github.com/HL7/fhir-ig-publisher/releases/latest/download/publisher.jar -o ./publisher.jar --create-dirs

      - name: Install latest version of fsh-sushi
        run: npm install -g fsh-sushi@latest
        
      - name: Check sushi version
        run: sushi -v
        

      - name: Checkout source repo to ./source
        uses: actions/checkout@v2
        with:
          repository: IHE/RAD.IRA
          path: source

      - name: Check sushi version
        run: sushi -v

      - name: Checkout HL7/fhir-ig-history-template to ./history-template
        uses: actions/checkout@v2
        with:
          repository: HL7/fhir-ig-history-template
          path: history-template

      - name: Checkout publications repo to ./webroot
        uses: actions/checkout@v2
        with:
          repository: IHE/publications
          path: webroot

      - name: Checkout FHIR/ig-registry to ./ig-registry
        uses: actions/checkout@v2
        with:
          repository: FHIR/ig-registry
          path: ig-registry

      - name: Run publisher command
        run: java -jar ./publisher.jar publisher -ig core

      - name: Run publisher command for publishing release
        run: java -Dfile.encoding=UTF-8 -jar publisher.jar -go-publish -source source -web $(pwd)/webroot -registry ./ig-registry/fhir-ig-list.json -history ./history-template -templates $(pwd)/webroot/templates

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./webroot
